-- SQLite schema for research-jarvis (built out on Day 5).
-- Tables planned: projects (with is_active), conversations, messages, memory, tasks, cost_log.
-- Every memory/conversation row is project-scoped (Version_1_plan.md rule #5).
-- A default "general" project is seeded so the app is project-aware from day one.

-- (Schema definitions go here on Day 5.)
