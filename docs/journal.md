# Build Journal

One line per day. What got done, what broke, what's next.

- 2026-05-22 — Repo structure scaffolded from Version_1_plan.md. Folders + stub files in place.
- 2026-05-22 — Day 3: FastAPI skeleton, Pydantic Settings, loguru + request-ID ContextVar, all endpoints stubbed with real response shapes, frontend ping + WebSocket wiring.
- 2026-05-22 — Day 4: LLM provider abstraction. Switched deprecated google-generativeai → google-genai. GeminiProvider + LLMRouter + cost_tracker stub. /chat returns real Gemini answers. Fallback switched from OpenAI (quota exhausted) to Groq llama-3.3-70b-versatile (free tier, groq_llm.py). Fallback chain confirmed.
- 2026-05-23 — Day 5: SQLite schema + project-scoped storage. 6 tables (projects, conversations, messages, memory, tasks, cost_log), default "general" project seeded. /chat now persists user + assistant turns; cost_tracker writes to cost_log. scripts/set_project.py for CLI project switching. Gotcha: PRAGMA foreign_keys = ON is a no-op inside Python's implicit transactions — fix is to run it via executescript() after the schema. CLI sqlite3 also lies about FK state (shows its own connection); verify from Python.
- 2026-05-23 — Day 6: ChromaDB semantic memory. vector_store.py (per-project collections, cosine distance, Gemini embeddings), importance.py (LLM 1-10 scorer, fail-safe 0 return), save_memory() added to sqlite_store. /chat now retrieves top-3 relevant memories pre-LLM and stores important exchanges post-LLM. Gotcha: text-embedding-004 retired — replaced by gemini-embedding-001 (3072-dim). Gotcha: chromadb.PersistentClient is a factory function in v1.x, not a class — use chromadb.api.ClientAPI for type hints. Memory injection inflates trivial message scores (accepted as correct behaviour).
- 2026-05-23 — Day 7: PyWebView shell + global hotkeys. Transparent always-on-top frameless window, bottom-right positioned. pynput Listener (OS thread) → asyncio.Queue → _drain_events task → WebSocket broadcast → React status badge. Decisions: B2 queue pattern, A2 accept system-menu flash. Gotchas: backend/desktop.py shadowed by backend/desktop/ package — fixed by moving launcher to __main__.py; Ctrl+Alt+J detection needs key.vk not key.char (Ctrl modifier corrupts char to control character); Edge WebView2 resolves localhost to IPv6 for WebSocket but uvicorn binds IPv4 — fixed with ws://127.0.0.1; WS hook needs reconnect loop (backend not ready on first page load).
- 2026-05-23 — Day 8: PTT audio capture with sounddevice. AudioRecorder (16kHz mono int16, list-of-arrays buffer, threading.Lock, WAV via stdlib wave). _drain_events renamed to _dispatch_events — now branches on event type: ptt_start → start_recording (run_in_executor), ptt_end → stop + save WAV to data/recordings/, inject recording_saved event. app.state.ready flag added (Day 7 carryover). data/settings.json-backed runtime config for mic device choice. GET/POST /audio/device + GET /audio/devices endpoints. Frontend: recording_saved flashes filename in badge for 2s. Ready for Day 9 STT.
- 2026-05-23 — Day 9: Groq Whisper STT wired into dispatcher. STTService (AsyncGroq, whisper-large-v3, per-call latency logging, single STTError for sanitised UI messages). recording_saved branch in dispatcher: broadcasts transcribing → Groq call → transcription_complete{text, latency_ms} or transcription_failed{error}. Frontend: VoiceEvent union extended, ChatPanel component added for user transcript bubbles, red error toast auto-fades after 3s. Gotcha: React 18 automatic batching dropped the transcription_failed event when recording_saved arrived immediately after — fixed by not re-broadcasting recording_saved from outer dispatch loop. Empty-audio mis-press now surfaced as transcription_failed rather than silently dropped. All 3 error paths verified: bad key, network down, empty audio.
- 2026-05-23 — Day 10: Piper TTS wired through chat. TTSService (asyncio subprocess, --output_raw PCM, sounddevice via run_in_executor, single TTSError, latency logged on both success/failure). Settings extended with 5 TTS fields (piper_binary_path, piper_voice_path, tts_sample_rate, tts_output_device, tts_timeout_seconds). Lifespan gates app.state.ready on recorder + stt + tts; shuts down LIFO. POST /speak debug endpoint (synchronous, 1-2000 char validated, 503 on TTSError). /chat fires asyncio.create_task(_speak_safely(...)) after returning — text appears immediately, audio follows. Gotcha: sd.play() with explicit dtype= conflicts when array is already np.int16 — removed redundant kwarg. All 6 edge cases verified: short, medium, 30-word buffer test (ends cleanly on "cases"), technical vocab, numerics, whitespace-only → 422.
