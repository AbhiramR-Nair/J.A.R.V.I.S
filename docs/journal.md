# Build Journal

One line per day. What got done, what broke, what's next.

- 2026-05-22 — Repo structure scaffolded from Version_1_plan.md. Folders + stub files in place.
- 2026-05-22 — Day 3: FastAPI skeleton, Pydantic Settings, loguru + request-ID ContextVar, all endpoints stubbed with real response shapes, frontend ping + WebSocket wiring.
- 2026-05-22 — Day 4: LLM provider abstraction. Switched deprecated google-generativeai → google-genai. GeminiProvider + LLMRouter + cost_tracker stub. /chat returns real Gemini answers. Fallback switched from OpenAI (quota exhausted) to Groq llama-3.3-70b-versatile (free tier, groq_llm.py). Fallback chain confirmed.
- 2026-05-23 — Day 5: SQLite schema + project-scoped storage. 6 tables (projects, conversations, messages, memory, tasks, cost_log), default "general" project seeded. /chat now persists user + assistant turns; cost_tracker writes to cost_log. scripts/set_project.py for CLI project switching. Gotcha: PRAGMA foreign_keys = ON is a no-op inside Python's implicit transactions — fix is to run it via executescript() after the schema. CLI sqlite3 also lies about FK state (shows its own connection); verify from Python.
- 2026-05-23 — Day 6: ChromaDB semantic memory. vector_store.py (per-project collections, cosine distance, Gemini embeddings), importance.py (LLM 1-10 scorer, fail-safe 0 return), save_memory() added to sqlite_store. /chat now retrieves top-3 relevant memories pre-LLM and stores important exchanges post-LLM. Gotcha: text-embedding-004 retired — replaced by gemini-embedding-001 (3072-dim). Gotcha: chromadb.PersistentClient is a factory function in v1.x, not a class — use chromadb.api.ClientAPI for type hints. Memory injection inflates trivial message scores (accepted as correct behaviour).
- 2026-05-23 — Day 7: PyWebView shell + global hotkeys. Transparent always-on-top frameless window, bottom-right positioned. pynput Listener (OS thread) → asyncio.Queue → _drain_events task → WebSocket broadcast → React status badge. Decisions: B2 queue pattern, A2 accept system-menu flash. Gotchas: backend/desktop.py shadowed by backend/desktop/ package — fixed by moving launcher to __main__.py; Ctrl+Alt+J detection needs key.vk not key.char (Ctrl modifier corrupts char to control character); Edge WebView2 resolves localhost to IPv6 for WebSocket but uvicorn binds IPv4 — fixed with ws://127.0.0.1; WS hook needs reconnect loop (backend not ready on first page load).
- 2026-05-23 — Day 8: PTT audio capture with sounddevice. AudioRecorder (16kHz mono int16, list-of-arrays buffer, threading.Lock, WAV via stdlib wave). _drain_events renamed to _dispatch_events — now branches on event type: ptt_start → start_recording (run_in_executor), ptt_end → stop + save WAV to data/recordings/, inject recording_saved event. app.state.ready flag added (Day 7 carryover). data/settings.json-backed runtime config for mic device choice. GET/POST /audio/device + GET /audio/devices endpoints. Frontend: recording_saved flashes filename in badge for 2s. Ready for Day 9 STT.
- 2026-05-23 — Day 9: Groq Whisper STT wired into dispatcher. STTService (AsyncGroq, whisper-large-v3, per-call latency logging, single STTError for sanitised UI messages). recording_saved branch in dispatcher: broadcasts transcribing → Groq call → transcription_complete{text, latency_ms} or transcription_failed{error}. Frontend: VoiceEvent union extended, ChatPanel component added for user transcript bubbles, red error toast auto-fades after 3s. Gotcha: React 18 automatic batching dropped the transcription_failed event when recording_saved arrived immediately after — fixed by not re-broadcasting recording_saved from outer dispatch loop. Empty-audio mis-press now surfaced as transcription_failed rather than silently dropped. All 3 error paths verified: bad key, network down, empty audio.
- 2026-05-23 — Day 10: Piper TTS wired through chat. TTSService (asyncio subprocess, --output_raw PCM, sounddevice via run_in_executor, single TTSError, latency logged on both success/failure). Settings extended with 5 TTS fields (piper_binary_path, piper_voice_path, tts_sample_rate, tts_output_device, tts_timeout_seconds). Lifespan gates app.state.ready on recorder + stt + tts; shuts down LIFO. POST /speak debug endpoint (synchronous, 1-2000 char validated, 503 on TTSError). /chat fires asyncio.create_task(_speak_safely(...)) after returning — text appears immediately, audio follows. Gotcha: sd.play() with explicit dtype= conflicts when array is already np.int16 — removed redundant kwarg. All 6 edge cases verified: short, medium, 30-word buffer test (ends cleanly on "cases"), technical vocab, numerics, whitespace-only → 422.
- 2026-05-24 — Day 11: Full PTT voice loop with state machine and mute. ConversationOrchestrator (IDLE→LISTENING→TRANSCRIBING→THINKING→SPEAKING→IDLE + MUTED + ERROR) owns the complete pipeline; dispatcher is now a thin routing layer. useVoiceEvents hook refactored from lastEvent to useReducer queue (no more dropped back-to-back events). WS event contract formalised (StateChangedEvent, AssistantMessageEvent, SpeakingStarted/Ended/Failed). Memory: _build_context (last-4 + top-3 semantic, deduped, char-capped), _persist_turn (SQLite always + ChromaDB if importance≥4). Five race-condition guards (G1–G5) verified. ERROR auto-recovers to IDLE after 3s. Mute cancels in-flight tasks and stops TTS playback (sd.stop via executor). Frontend shows live voiceState label from state_changed events. Week 2 milestone: PTT → spoken answer end-to-end.
- 2026-05-25 — Day 12: Audio robustness. _handle_error lock assertion added; recordings pruned by age (14-day default, backend/voice/cleanup.py). POST /audio/device now test-opens the new device before swapping. 30s PTT cap fires a recording_cap_hit event + UI toast instead of silently discarding. Mic disconnect mid-recording raises AudioCaptureError via callback_error flag, orchestrator shows error + auto-recovers; on_ptt_start rebuilds recorder on default device if open fails. Test mic button in SettingsPanel (3s record+playback, peak amplitude badge). PortAudio errors classified by MME error code — mme error 1 maps to privacy-denied, unanticipated host error maps to in-use. Verified: happy path, 30s cap, mic disconnect, test mic (loud/silent), privacy denied. Next: buffer Day 13.
- 2026-05-25 — Day 13: Swapped Piper voice to en_GB-alan-medium (22050 Hz verified; lessac retained as fallback). Added modular JARVIS personality — six markdown files under backend/prompts/system/ assembled at startup by loader.py into JARVIS_SYSTEM_PROMPT, wired via system_instruction on Gemini. Debug endpoint at GET /debug/system-prompt. All 7 persona probes passed; Alan's register pairs well with the JARVIS persona. Day 14 remains buffer.
- 2026-05-26 — Day 14: Week 2 buffer close-out. scripts/download_models.py written (Piper binary + both voices); scripts/setup_windows.ps1 written (UTF-8 encoding, venv, npm, data dirs, .env copy, model download). logger.contextualize(request_id=turn_id) wraps full pipeline in _run_pipeline — one searchable ID per voice turn. Fixed mute-toggle UI bug (mute_toggle event was toggling muted back to true after state_changed already cleared it). Demo video recorded. Tagged v0.2.0-voice-loop.
- 2026-05-27 — Day 15: SVG/CSS animated blob. BlobStates.ts (BlobStateConfig interface, BLOB_STATES record, mapVoiceStateToVisualState). Blob.tsx: rAF loop for morphing/conic rotation/specular drift, Framer Motion useMotionValue+animate() for 600ms state transitions, 4-layer stack (conic gradient → radial regions → specular → feTurbulence grain). 6 visual states all distinct and smooth. Dev-only state cycler in App.tsx. Pre-flight: removed redundant muted boolean, deleted placeholder test circle, git rm'd empty useVoiceState.ts + StatusBar.tsx. CPU: ~13% on msedgewebview2.exe in idle (target was <10% — marginal overage, review on Day 16).
- 2026-05-27 — Day 16: Audio reactivity. Orchestrator broadcasts amplitude at 20Hz via WebSocket (amplitude_broadcast_hz setting): mic RMS normalised against mic_calibration_max, Piper PCM chunks normalised against tts_calibration_max, EMA smoothing (α=0.3) on both. amplitudeRef side-channel in useVoiceEvents (never queued — latest value wins). Blob rAF loop reads amplitudeRef each frame and drives morphAmount + scale. StatusBar waveform shares same ref. Gotcha: sounddevice callback runs in a C thread — must use loop.call_soon_threadsafe to push amplitude into asyncio; direct queue.put_nowait from C thread corrupts the event loop. tts_calibration_max=0.3 tuned on Day 18; mic_calibration_max=0.1 confirmed good.
- 2026-05-28 — Day 17: HUD aesthetic foundation. Dev scaffolding stripped from App.tsx (state cycler, amplitude slider, ping button, debug labels). New components: HudFrame (4 L-bracket corner divs, shared CORNER_BASE with borderStyle:"solid" — omitting this makes borders invisible), HeaderBand (drag bar with identity left + gear/close right, WebkitAppRegion drag/no-drag split), StatusBar (HudFrame wrapper, Mic icon, 7-bar waveform RAF loop with voiceStateRef pattern, per-state accent colour + subtext). ChatPanel + SettingsPanel fully rewritten with HudFrame treatment. Project switcher: no /projects endpoints existed — built backend/api/projects.py (GET /projects, POST /projects/active) as thin wrappers over existing sqlite_store functions. Dark background: #060d14 on #root (prevents WebView2 white flash before mount) + App root div; html/body stay transparent for PyWebView overlay. Gotcha: wordBreak:"break-words" is a Tailwind class name not a CSS value — correct CSS is "break-word". SettingsPanel root is motion.div so AnimatePresence exit animation fires correctly.
- 2026-05-28 — Day 18: Window polish. Snap-to-corner: SnapManager with 150ms debounce (window.events.moved fires continuously during drag in PyWebView 6.x WinForms — not drag-end only as plan assumed); 60px center-to-center threshold; 8px margin; atomic write-rename to data/window_state.json; 500ms cooldown prevents snap→move→moved→snap loop. Minimize-to-tray: TrayManager (pystray 0.19.5, daemon thread, Show/Quit menu, 64×64 cyan PNG icon via Pillow). POST /window/hide hides overlay — backend + hotkeys keep running. Gotcha: storing singleton in __main__.py fails because python -m backend.desktop registers the module as sys.modules['__main__'] not sys.modules['backend.desktop.__main__'] — cross-module imports of __main__ get a fresh copy with None. Fix: singleton stored in backend/desktop/tray.py (set_instance/get_instance) which is always imported by the same path. Connection-state dot: ConnectionState type + retry counter ref in useVoiceEvents; 5 consecutive failures escalates amber→red; resets on reconnect. Amber CSS pulse animation during reconnecting. StatusBar RAF idle guard landed (Day 17 carryover). TTS calibration 0.3 confirmed. Idle CPU: 16% combined (target was <10%; RAF idle guard reduces spike but doesn't hit target at idle). Tagged for v0.3.0-blob at end of Day 19.
- 2026-05-28 — Day 19: Week 3 close-out + Day 20 design. Tagged v0.3.0-blob. google-genai 2.6.0 SDK source read: key finding is `parameters_json_schema=` (dict) not `parameters=` (types.Schema) in FunctionDeclaration — hand-written schemas pass straight through. 8 design Q&A for tool-calling arch recorded in day_20_plan.md: typed union (Q2), singleton in __init__.py not __main__ (Q4, applies Day 18 lesson), _validate_schema as named method (Q5), lazy SDK import in gemini_function_schemas (Q6), hard/soft error split (Q7). Stub files written: backend/tools/__init__.py (registry singleton) + registry.py (full signatures, NotImplementedError bodies). Multi-turn contents list design documented: raw.candidates[0].content must be preserved exactly between iterations or Gemini raises.
- 2026-05-28 — Day 20: Tool-calling architecture complete. LLMResponse dataclass → TextResponse | ToolCallResponse discriminated union (base.py, gemini.py, router.py, groq_llm.py committed atomically). ToolRegistry: _validate_schema() rejects $defs/$ref/allOf/oneOf/anyOf at registration time, @register decorator (async enforcement, duplicate check), gemini_function_schemas() builds single types.Tool with all declarations, execute() hard/soft error split (ToolNotFoundError/ToolSchemaError re-raise; all others return {"error":...} soft). Tool-call loop replaces single LLM call in conversation.py: MUTED re-check before each iteration, contents list grows by two entries per round-trip, Q8 fallback fires tools=None call if loop exhausts. get_current_time first real tool; voice smoke test confirmed: "what time is it?" → tool_call iter=0: get_current_time({}) → spoken time. tool-calling-pattern/SKILL.md written (4-step pattern, schema rules, hard/soft reference, 7 gotchas).
- 2026-05-29 — Day 21: Project memory tools. Four tools shipped: list_projects (returns names with (active) marker), set_active_project (get-or-create atomic in sqlite_store — no new helper needed), log_to_project (ChromaDB write non-fatal, SQLite always writes, importance=10 hard-coded), recall_from_project (project_id-scoped vector search — the isolation invariant). StatusBar chip [projectname] added; project_changed WS event broadcast from orchestrator after set_active_project tool succeeds; also sent on WS connected so chip initialises without a REST poll. Gotcha: Gemini 2.5 Flash free tier = 20 RPD (preview cap, not 1500 RPD as plan assumed) — exhausted during testing; 2.0 Flash also hit quota; switched to gemini-flash-lite-latest (only model available). Gotcha: API_BASE used http://localhost:8000 — Edge WebView2 resolves localhost → IPv6 ::1 but uvicorn binds IPv4 only, silently failing all REST fetches; fixed to 127.0.0.1 (same reason already documented on WS_VOICE_URL). Fix: name.strip().lower() in set_active_project — STT phonetic variants (Kainez, Kines) no longer create duplicate projects. Cross-project isolation verified: ChromaDB per-project collections hold; beta facts did not appear in alpha recall.
- 2026-05-29 — Day 22: PDF parsing foundation. backend/models/pdf.py (ParsedPaper, PaperSection, PaperChunk Pydantic models). backend/tools/pdf_parser.py: parse_pdf() with title heuristic (largest font page 1), abstract regex, section detection, paragraph-boundary chunking. Handles: FileNotFoundError, PDFParseError (encrypted/corrupt), is_scanned=True (no text layer). Smoke test passes across clean paper, long paper, blank-page scanned mock, bad path, corrupt file. tools registered: 5 unchanged (pdf_parser is library code, not a tool — Day 23 registers summarize_paper). Decided: PDF image/OCR/multimodal deferred to v2. Gotcha: ALL-CAPS section regex initially matched 3-char abbreviations (PEG, DNA, USA) — 167 false sections on a 29-page paper. Fix: \s*$ end-of-line anchor (headers sit alone on their line; abbreviations don't) + min 5-char ALL-CAPS + body >= 100 chars filter → 3–7 genuine sections. gemini_model_heavy switched to gemini-2.5-flash (available); gemini_model stays on gemini-flash-lite-latest to conserve quota.
